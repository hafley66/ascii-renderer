# p02__glm53f-max-c prompt sequence

Verbatim prompts and steering messages for the `isfahan` mode run, in order.
Maintained by lane art/p02-glm53f-max-c. Nothing here is paraphrased; entries
are the exact text received by the lane.

## 1. 2026-09-20T00:37:42-0400 | pitch brief (lanes/3_p02_glm53f_max.PITCH.md)

# Crazy art one-shot, prompt 2 (pitch)

One sentence per thing.

- Mission: create the most visually ambitious finished ASCII art mode this repository can support, in one lane, from inspection through implementation, visual refinement, validation, and commits.
- Plan: open with the visual thesis and a stratified plan of 4 to 6 ordered commit checkpoints, each naming its goal, deliverable, files, commit subject, validation, and yield, and send it to the parent through `boop beep parent --no-wait` before touching a file.
- Yields: after the go, take that plan and move with it, committing and sending the receipt (`status`, `checkpoint`, `sha`, `files`, `validation`, `next`) to the parent at every checkpoint and ending your turn until it replies, one checkpoint per turn.
- Divergence: yield immediately if reality diverges, with what changed, why, and the amended remaining checkpoints, and never silently re-plan.
- Prompt sequence: keep `briefs/<ART_RUN_NAME>.PROMPTS.md` verbatim, in order, with timestamps, committed by the checkpoint where focused tests first pass and completed before the final commit, because art without its prompt sequence is not done.
- Artistic brief: invent one original animated mode with a legible silhouette, deliberate negative space, multi-scale detail, a coherent glyph vocabulary, intentional color hierarchy, seeded structural variation, and motion that reveals the system.
- Region blend: pick one of Japan, India, China, Russia, Europe, Africa, or Iran, extract the geometry only it uses and one of its famous pattern traditions, and blend them algorithmically into one animated geometric artwork that is exciting and a joy to look at, with the pattern generated by rules rather than stamped.
- Continuous system: build one coupled generative system whose continuous controls deform the same structure and whose subsystems exchange state, with no enum menus, no composited modes, and no uniform noise.
- Repository contract: read AGENTS.md, use the add-mode/add-animation/add-sprite-algo skills, write the single mode file at `ART_MODE_PATH`, run `scripts/0_generate_modes.sh`, never hand-edit `src/modes/mod.rs` or legacy dispatch, and preserve every existing mode.
- Design before body: state the type signatures and the twelve design points (lifetime, state, identity and RNG, read-write order, branching, uniqueness, bounds, clipping, layers, animation invariants, parameter mapping, feedback) before writing the body.
- Animation: motion must express the system and stay stable across adjacent frames, with live `Mode::params` knobs matching registry defaults.
- Visual loop: inspect 80x24, one larger frame, one clipping case, three animation phases, three seeds, and a five-value sweep with adjacent midpoint values, then refine.
- Testing: add fixed-seed snapshots for the canonical frame, two phases, one parameterized variant, one clipping case, and two nearby control values; inspect each visually; run `cargo test` and `scripts/13_e2e.sh` when live behavior changes, reporting unexecuted cases.
- Performance: reuse the probe guard and layer timers, bound all growth, keep every limit fixed, and record each command with its observed result.
- Commits: commit at each checkpoint and each milestone, green and scoped, with `Boop-Status` and `Boop-Check` trailers, the prompt record landing with the first passing-tests checkpoint, and `Boop-Status: done` on the final commit.
- Finish line: the mode registers, renders, varies by seed, moves coherently, carries inspected snapshots and a complete prompt sequence, passes `cargo test` and applicable E2E within guards, follows the approved plan, leaves a clean worktree, and yields the final receipt (mode, files, snapshots, continuity, emergence, prompts, validation, e2e, performance, art, next: parent).
- Blocked: commit the exact reproduction with `Boop-Status: blocked` and one `Boop-Ask` question, and never substitute a reduced finish line without approval.

## 2. 2026-09-20T00:49:12-0400 (recorded at receipt) | parent go (boop m-6b38f7da from herd)

Go: plan approved with two corrections. (1) Prompt record: your lane received the pitch brief only. Entry 1 is that file verbatim, read /Users/chrishafley/projects/ascii-renderer/lanes/3_p02_glm53f_max.PITCH.md, then this reply and later parent messages in order. There is no prompt 1; record nothing you did not receive. (2) Modes list: follow AGENTS.md, query registry names from code; do not edit the stale list in CLAUDE.md. Execute checkpoint 1 now. At every checkpoint: finish, validate, commit, hail the receipt with boop beep parent --no-wait, then end your turn for the next go. Keep briefs/p02__glm53f-max-c.PROMPTS.md current and verbatim.

## 3. 2026-09-20T05:14:30-0400 (recorded at receipt) | parent go for checkpoint 2 (boop m-02ebd495 from herd)

Checkpoint 1 verified: commit 8cfbd6f, 749-line isfahan mode, focused and cli suites green, the two full-suite failures correctly attributed to pre-existing causes via stash, snapshot reads as girih strapwork inside the portal outline. Go for checkpoint 2/5: muqarnas crown, shadow bands, glyph and color hierarchy. Same protocol: commit, hail the receipt, end your turn.

## 4. 2026-09-20T05:41:10-0400 (recorded at receipt) | parent go for checkpoint 3 (boop m-08895793 from herd)

Checkpoint 2 verified: commit dac1186, module and CLI green, the pre-existing failures attributed correctly, and the crown tiers read as concentric rings with radial seams. Go for checkpoint 3/5 (subdivision-wave animation and live knobs). Same protocol: commit, hail the receipt, end your turn.

## 5. 2026-09-20T06:12:40-0400 (recorded at receipt) | parent go for checkpoint 4 (boop m-686add2f from herd)

Checkpoint 3 verified: commit 0c407dc, the lantern wave drives flash, shimmer and twinkle coherently, phase snapshots at t4 and t9. Go for checkpoint 4/5 (seeded structural variation and sweep hardening: 3-seed snapshots, 5-value sweep with adjacent midpoints, frame_cost budget, layer coverage). Same protocol: commit, hail the receipt, end your turn.
