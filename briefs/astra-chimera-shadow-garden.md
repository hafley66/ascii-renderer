# Domain expansion: chimera shadow garden

Add one new file-owned generated-registry mode named `chimera-shadow-garden`.
Its title is:

```text
domain expansion: chimera shadow garden
```

The title is the full reference. Do not explain, summarize, identify, or add
source-material details. Make an original animated ASCII art mode from the
title alone.

The output should have a readable composition, layered depth, and visible
parameter-driven variation in the demo. Keep animation deterministic as a
pure function of time.

Read `AGENTS.md` first. Follow the generated registry workflow: choose the
next available numbered `src/modes/_N_name.rs` file, declare visible controls
in `Mode::params`, add fixed-seed snapshots, run
`scripts/0_generate_modes.sh`, and do not edit `src/modes/mod.rs` by hand.

Inspect static and nonzero-time renders. Accept only the snapshots belonging
to this mode. Run relevant tests. Commit all mode artifacts with this exact
subject:

```text
feat: chimera shadow garden
```

In the completion response, provide the mode name, controls, render commands,
test results, and commit hash.
