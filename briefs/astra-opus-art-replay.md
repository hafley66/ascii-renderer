# Opus art replay source

Direct user directions from the parent session
`f4c77e27-2e2f-464f-b16b-3f2675f87a24`:

```text
okay spawn 1 more fable to make incredible avant garde art im talking juice it
to the tits with cool geometry ascii art animation, spawn 2 of them in diff
files
```

```text
then have 2 opus make art, then 2 sonnet, then 2 haiku's. same open prompts
and guided new file for them to change with instructions across the board, they
all should prefix with a name and have a number/designation in title
```

```text
roll 3 more fables and 3 more opus, give them same open prompt, get all into
main
```

## File lease contract

Create one original animated ASCII art mode from the prompt chain above.

Use this temporary file name while creating the mode:

```text
src/modes/_D_next.rs
```

Do not choose a numeric prefix and do not generate `src/modes/mod.rs`. The
primary checkout leases the next numeric file name after delivery, renames the
source and its snapshots, runs the registry generator, profiles the mode,
installs the selected build, and commits the integrated result.

Write only `src/modes/_D_next.rs` and any prose receipt needed to describe its
mode name, controls, and intended snapshots. Do not edit existing source,
generated registry files, snapshots, or build configuration. The source file
must define its `Mode`, rendering function, parameter declaration, and
deterministic time behavior completely enough for the primary checkout to
rename and register it without design changes.

Commit the draft with this exact subject:

```text
feat: astra opus replay draft
```

On completion, report the chosen mode name, intended filename after leasing,
parameter keys, static and moving render commands, and commit hash.
