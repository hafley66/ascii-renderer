# p02__glm53f-max-d prompt sequence

Verbatim prompts and steering for this lane's art, in order. Timestamps UTC,
marked approx where the receipt was not instrumented. Kept current through the
final commit per the repo ground rules.

## Prompt 1, boop-start transport probe, ~2026-09-20T04:41Z (approx)

This is a transport readiness probe. Do not inspect the repository.
Respond exactly with: boop
Any tool call fails the probe. Do not reason, explain, or add punctuation.

(worker reply: boop)

## Prompt 2, pitch, ~2026-09-20T04:42Z (approx, immediately before plan send at 04:42:46Z)

# Crazy art one-shot, prompt 2 (pitch)

One sentence per thing.

- Mission: create the most visually ambitious finished ASCII art mode this repository can support, in one lane, from inspection through implementation, visual refinement, validation, and commits.
- Plan: open with the visual thesis and a stratified plan of 4 to 6 ordered commit checkpoints, each naming its goal, deliverable, files, commit subject, validation, and yield, and send it to the parent through `boop beep parent --no-wait` before touching a file.
- Yields: after the go, take that plan and move with it, committing and sending the receipt (`status`, `checkpoint`, `sha`, `files`, `validation`, `next`) to the parent at every checkpoint and ending your turn until it replies, one checkpoint per turn.
- Divergence: yield immediately if reality diverges, with what changed, why, and the amended remaining checkpoints, and never silently re-plan.
- Prompt sequence: keep `briefs/<ART_RUN_NAME>.PROMPTS.md` verbatim, in order, with timestamps, committed by the checkpoint where focused tests first pass and completed before the final commit, because art without its prompt sequence is not done.
- Artistic brief: invent one original animated mode with a legible silhouette, deliberate negative space, multi-scale detail, a coherent glyph vocabulary, intentional color hierarchy, seeded structural variation, and motion that reveals the system.
- Region blend: pick one of Japan, India, China, Russia, Europe, Africa, or Iran, extract the geometry only it uses and one of its famous pattern traditions, and blend them algorithmically into one animated geometric artwork that is exciting and a joy to look at, with the pattern generated by rules rather than stamped.
- Continuous system: build one coupled generative system whose continuous controls deform the same structure and whose subsystems exchange state, with no enum menus, no composited modes, and no uniform noise.
- Repository contract: read AGENTS.md, use the add-mode/add-animation/add-sprite-algo skills, write the single mode file at `ART_MODE_PATH`, run `scripts/0_generate_modes.sh`, never hand-edit `src/modes/mod.rs` or legacy dispatch, and preserve every existing mode.
- Design before body: state the type signatures and the twelve design points (lifetime, state, identity and RNG, read-write order, branching, uniqueness, bounds, clipping, layers, animation invariants, parameter mapping, feedback) before writing the body.
- Animation: motion must express the system and stay stable across adjacent frames, with live `Mode::params` knobs matching registry defaults.
- Visual loop: inspect 80x24, one larger frame, one clipping case, three animation phases, three seeds, and a five-value sweep with adjacent midpoint values, then refine.
- Testing: add fixed-seed snapshots for the canonical frame, two phases, one parameterized variant, one clipping case, and two nearby control values; inspect each visually; run `cargo test` and `scripts/13_e2e.sh` when live behavior changes, reporting unexecuted cases.
- Performance: reuse the probe guard and layer timers, bound all growth, keep every limit fixed, and record each command with its observed result.
- Commits: commit at each checkpoint and each milestone, green and scoped, with `Boop-Status` and `Boop-Check` trailers, the prompt record landing with the first passing-tests checkpoint, and `Boop-Status: done` on the final commit.
- Finish line: the mode registers, renders, varies by seed, moves coherently, carries inspected snapshots and a complete prompt sequence, passes `cargo test` and applicable E2E within guards, follows the approved plan, leaves a clean worktree, and yields the final receipt (mode, files, snapshots, continuity, emergence, prompts, validation, e2e, performance, art, next: parent).
- Blocked: commit the exact reproduction with `Boop-Status: blocked` and one `Boop-Ask` question, and never substitute a reduced finish line without approval.

## Worker plan sent to parent, 2026-09-20T04:42:46Z (m-e1577896, original Iran design, amended by prompt 3)

P02 plan: GIRIH (Iran). Thesis: one coupled system where a Safavid muqarnas dome depth-field breathes beneath rule-woven girih strapwork whose khatam rays reflect between the dome tiers themselves, so architecture generates ornament, ornament lights architecture, and kond point-packing fills the negative space it is repelled from.

DESIGN. Region geometry unique to Iran: muqarnas stepped vault + 10-fold khatam decagon. Pattern tradition: girih star-and-polygon strapwork, generated by reflection rules, never stamped. Mode girih, file src/modes/_101_p02_glm53f_max_d.rs, AnimKind::Iterate, 7 knobs TILT STRAP STAR DOME FLOW PACK WEAVE.
Signatures: knobs(frame)->Knobs; dome_field(&mut Field, frame, k, t); strap_walk(&mut Grid, &Field, frame, k, t); point_pack(&mut Grid, &Field, k, t); kufic_border(&mut Grid, k, t).
Twelve points. Lifetime: no retained state, Field scratch allocated per render call. State: Field depth Vec f32 per cell, nothing global. Identity and RNG: painters draw no rng, all identity from splitmix hash of (seed, layer, ix, iy) per rosette. Read-write order: field pass writes depth only, dome paint writes base, straps overwrite with priority, crossings next, dots only into untouched cells, border last. Branching: zero enum menus, every knob is continuous math on the same structure. Uniqueness: glyph and color from direction and phase, quantized only at dome tier steps, no uniform noise. Bounds: field sampled clamped, walkers step-checked, warp source coords bounds-checked. Clipping: 20x8 and 400x120 both terminate. Layers: 6 measure_layer painters, muqarnas_field, dome_paint, girih_straps, weave_crossings, kond_pack, kufic_border, covering 85 pct plus. Animation invariants: t 0 static and byte-deterministic, phases = hash(seed, layer, index) + t*FLOW, all time terms continuous sin for adjacent-frame stability. Parameter mapping: positional arg, then param_values, then ASCII_P_ env, clamped, registry defaults equal renderer fallbacks. Feedback: dome tiers set girih ring radii, strap depth-sample sets brightness and lateral refraction, dot placement reads strap occupancy and repels.

CHECKPOINTS.
C1 Foundation dome. Deliverable: file scaffold, PARAMS, knobs, muqarnas field and paint, layer timers, scripts/0_generate_modes.sh run, CLAUDE.md list append, briefs/p02__glm53f-max-d.PROMPTS.md with prompts 1-2 verbatim and timestamps. Files: src/modes/_101_p02_glm53f_max_d.rs, src/modes/mod.rs generated, CLAUDE.md, briefs, snapshots. Commit: Girih dome scaffold with muqarnas field. Validation: focused cargo test girih green, cargo run -- 42 girih inspected. Yield: registered mode with a living dome field.
C2 Strapwork. Deliverable: khatam ray reflection generator, strap raster, over-under weave crossings, dome-to-strap coupling. Commit: Weave girih strapwork over the dome. Validation: t0 and 200x60 snapshots inspected, focused tests green. Yield: legible interlaced silhouette.
C3 Motion. Deliverable: FLOW phase travel, tier breathing, STAR twist drift, PACK dots with strap repulsion, phase snapshots. Commit: Animate girih construction light. Validation: time-sensitivity tests, three-phase snapshots, scripts/13_e2e.sh within probe guard. Yield: motion that reveals the coupled system.
C4 Refinement. Deliverable: 3 seeds, clipping case, five-value adjacent-midpoint knob sweep, glyph and color hierarchy polish. Commit: Refine girih hierarchy. Validation: inspected sweep, snapshots updated deliberately. Yield: finished look.
C5 Close. Deliverable: full test matrix, canonical, two phases, parameterized variant, clipping, two nearby controls, seed sensitivity, determinism, frame_cost; perf layer_coverage and knob_sweep receipts into briefs/p02__glm53f-max-d.md; PROMPTS.md completed; full cargo test; clean worktree. Commit: Finish girih dome mode with Boop-Status done. Validation: whole suite green, receipts recorded. Yield: final receipt.

I end my turn after each checkpoint receipt and wait for your reply. No files touched before this message.

## Prompt 3, parent go with region amendment, ~2026-09-20T04:44Z (approx, message in hand at 04:45:05Z)

[boop m-84c0ffa1 from herd] Go, with one amendment before checkpoint 1: for spread across the four sibling runs, your region must not be Iran or India (taken). Choose one of Japan, China, Russia, Europe, or Africa; update the pattern tradition and the registry mode name to match, keep the rest of your plan shape, and state the amended region and tradition at the top of your checkpoint 1 receipt. Then execute checkpoint 1. At every checkpoint: finish, validate, commit, hail the receipt with boop beep parent --no-wait, then end your turn for the next go. Keep briefs/p02__glm53f-max-d.PROMPTS.md current and verbatim, including your plan and this message.

## Prompt 4, parent go for checkpoint 2, ~2026-09-20T05:12Z (approx, m-720e014a)

[boop m-720e014a from herd] Checkpoint 1 verified: commit 4a06e5d, 7/7 module and 2/2 integration green, amended region China accepted; the moon gate ring, ice-ray chords and hui-wen frets read clearly at 80x24. Go for checkpoint 2/5 (meihua blooms on junctions wired to BLOOM, xiangyun clouds inside the gate wired to DRIFT). Same protocol: commit, hail the receipt, end your turn.

## Prompt 5, parent go for checkpoint 3, ~2026-09-20T05:21Z (approx, m-b3aeef5c)

[boop m-b3aeef5c from herd] Checkpoint 2 verified: commit b4c3f22, 10/10 module and 2/2 integration green, meihua nodes read at the crossings and the xiangyun band holds under the ring; the joint-as-flower pivot matches real lattice construction. Go for checkpoint 3/5 (motion depth: petal breathing, cloud curl animation, three-phase snapshots, scripts/13_e2e.sh under the probe guard). Same protocol: commit, hail the receipt, end your turn.
